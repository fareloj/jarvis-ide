---
id: project-memory
name: Memória do projeto
description: Consulta decisões e preferências persistentes do projeto.
---
Use `memory_list` para consultar decisões, requisitos e preferências persistentes. Nunca invente uma memória. Antes de gravar uma nova memória, explique o que será salvo e solicite aprovação usando `memory_save`.

